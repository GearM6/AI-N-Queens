package Queens;

import javafx.scene.Parent;
import javafx.scene.*;

public class Board extends Scene{
    public Board(Parent root, double width, double height) {
        super(root, width, height);
    }
    // Logical and graphical representations of board
}
